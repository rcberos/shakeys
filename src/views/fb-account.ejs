<table class="table table-hover table-striped">
	<thead>
		<th>Picture</th>
		<th>Name</th>
		<th>Button</th>
	</thead>
	<tbody>
		<tr ng-repeat="account in FB_Accounts">
			<td>
				<image src="{{account.picture.data.url}}" >
			</td>
			<td>
				{{account.name}}
			</td>
			<td>
				<button ng-click="get_page(account.id, account.access_token)">SELECT</button>
			</td>
		</tr>

	</tbody>

</table>